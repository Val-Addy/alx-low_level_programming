#include "main.h"

/**
 *_islower - check if char is lowercase
 *@c: character to check if lowercase 
 *Return; 0 if lowercase, 1 if not 
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (o);
	else
		return (1);

}	
